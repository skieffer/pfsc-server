name: pise-publish
on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'
jobs:
  build-and-test:
    uses: ./.github/workflows/pise-build-and-test.yml
    with:
      pub-prep: true
      v-tag: ${{github.ref_name}}
